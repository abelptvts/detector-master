import { DarkTheme } from "react-native-paper";

export const theme = {
    ...DarkTheme,
    dark: true,
    colors: {
        ...DarkTheme.colors,
        primary: "#ffd54f",
        accent: "#ffd54f",
    },
};
